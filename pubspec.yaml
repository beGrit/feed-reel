name: grit_soft_feed_reel
description: >-
  Feed Reel Video Player.
publish_to: 'none'
version: 0.1.0+1

homepage: https://github.com/beGrit/feed-reel

environment:
  sdk: "^3.11.5"
  flutter: "^3.41.7"

dependencies:
  flutter:
    sdk: flutter
  video_player: ^2.11.1
  audio_service: ^0.18.18
  audio_session: ^0.1.25
  freezed_annotation: ^3.0.0
  json_annotation: ^4.9.0
  provider: ^6.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.4.14
  freezed: ^3.0.0
  json_serializable: ^6.9.0

flutter:
  uses-material-design: true
  # assets:
  #   - packages/grit_soft_feed_reel/assets/images/default_cover.png
